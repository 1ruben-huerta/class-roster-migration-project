#pragma once 
#include <string>

enum DegreeProgram { SECURITY, NETWORK, SOFTWARE };

/*An array of string values to match each index value of degreeProgram array*/
static const std::string degreeProgramStrings[] = { "SECURITY", "NETWORK", "SOFTWARE" };